{
  "name": "frontend",
  "version": "1.0.0",
  "description": "npm install -g http-server\r http-server -p 3000",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server -p 3000",
    "deploy": "node bundle.js && node deploy.cjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
