{
  "name": "backend",
  "version": "0.1.0",
  "bin": {
    "backend": "bin/backend.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest",
    "synth:javascript": "cdk synth -c env=javascript",
    "deploy:javascript": "cdk deploy -c env=javascript"
  },
  "devDependencies": {
    "aws-cdk": "2.99.1",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.540.0",
    "@aws-sdk/lib-dynamodb": "^3.540.0",
    "aws-cdk-lib": "2.99.1",
    "constructs": "^10.0.0"
  }
}
