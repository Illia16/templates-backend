{
  "name": "backend",
  "version": "0.1.0",
  "bin": {
    "backend": "bin/backend.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest",
    "synth:javascript": "dotenv -e .env.javascript -- cdk synth --profile personal",
    "deploy:javascript": "dotenv -e .env.javascript -- cdk deploy --profile personal"
  },
  "devDependencies": {
    "aws-cdk": "^2.159.1",
    "dotenv-cli": "^7.4.2",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.159.1",
    "constructs": "^10.0.0"
  }
}
